# venv / build
.venv/
venv/
*.egg-info/
build/
dist/
__pycache__/
*.pyc
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage
.coverage.*

# secrets / credentials (R11: never commit token/secret values)
.env
.env.*
*.pem
*.key
*_token*
*secret*
.netrc

# large data / datasets / model weights (never vendored)
*.safetensors
*.ckpt
*.pt
*.pth
data/
datasets/
wandb/
outputs/
hf_cache/
.cache/

# vendored-inspection clones (never part of the package)
_vendor_*/
_inspect_*/

# eval artifacts that are regenerated (results JSON is committed selectively)
eval_runs/

# autonomous-build progress state (internal protocol bookkeeping, not part of the package)
.dtwarp-progress.json

# OS
.DS_Store
